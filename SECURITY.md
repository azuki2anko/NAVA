# セキュリティポリシー

## 対象バージョン

公開後は、最新リリースと`main`ブランチを修正対象とします。古いリリースへ個別に修正をバックポートする保証はありません。

## 脆弱性の報告

セキュリティ上の問題、秘密情報の露出、localhost APIの境界を越える問題は、公開Issueへ詳細を書かず、GitHubのPrivate vulnerability reportingから非公開で報告してください。

報告には、次の情報を可能な範囲で含めてください。

- 影響を受けるバージョンまたはコミット
- 再現条件と影響
- 最小限の再現手順
- 推奨する修正案（任意）

実IPアドレス、MACアドレス、機器ID、トークン、鍵、個人パス、実際の設定ファイルや無加工ログは添付しないでください。

## セキュリティ境界

- アンプのYamaha Extended Control APIは認証なしHTTPであるため、インターネットへ公開しないでください。
- アプリのREST APIは既定で`127.0.0.1`にだけバインドします。
- 本アプリは外部入力から任意URL、任意コマンド、任意キー列、任意Yamahaエンドポイントを実行しません。
- 未文書化APIは既定で使用しません。
- 初回の機器探索は読み取り専用です。

公開Issueへ誤って秘密情報を投稿した場合は、投稿を編集するだけでなく、該当するトークンや鍵を直ちに無効化・再発行してください。
